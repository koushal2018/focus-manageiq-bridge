apiVersion: v1
kind: Service
metadata:
  name: {{ include "finops.name" . }}
  labels: {{- include "finops.labels" . | nindent 4 }}
spec:
  selector: {{- include "finops.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 8000
      targetPort: http
  type: ClusterIP
