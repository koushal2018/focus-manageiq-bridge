# Slice 5 web layer
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
jinja2>=3.1,<4.0
psycopg2-binary>=2.9,<3.0

# Slice 7 Bedrock NL-query layer (optional --- the AI service is
# opt-in via env BEDROCK_DISABLED=0; the rest of the stack works
# without these installed, but `ai/` won't import without them).
boto3>=1.34,<2.0
sqlglot>=23.0,<27.0

# Production process manager: gunicorn supervises uvicorn workers.
gunicorn>=21.2,<24.0

