# Dev/CI-only dependencies — NOT installed into the production image.
# The Dockerfile installs only requirements.txt (non-root /opt/venv is
# read-only at runtime), so keeping the test runner and its HTTP test client
# out of that file keeps them out of the shipped artifact. CI installs both:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Test runner.
pytest>=8.0,<9.0

# In-process route testing (Starlette/FastAPI TestClient needs httpx).
httpx>=0.27,<1.0
